import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import './assets/reset';
import './middleware/setREM';

ReactDOM.render(<App />, document.getElementById('root'));
